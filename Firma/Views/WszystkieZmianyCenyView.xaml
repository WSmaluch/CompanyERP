<local:WszystkieViewBase x:Class="Firma.Views.WszystkieZmianyCenyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Firma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Path=IdZmianyCeny}"/>
                <DataGridTextColumn Header="Towar" Binding="{Binding Path=TowarNazwa}"/>
                <DataGridTextColumn Header="Jednostka miary" Binding="{Binding Path=JednostkaMiaryNazwa}"/>
                <DataGridTextColumn Header="CenaNetto" Binding="{Binding Path=CenaNetto}"/>
                <DataGridTextColumn Header="DataObowiazywaniaOd" Binding="{Binding Path=DataObowiazywaniaOd}"/>
                <DataGridTextColumn Header="DataObowiazywaniaDo" Binding="{Binding Path=DataObowiazywaniaDo}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
